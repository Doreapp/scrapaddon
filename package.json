{
    "scripts": {
        "lint": "web-ext lint --source-dir ./gitlab-stats/",
        "build": "web-ext build --source-dir ./gitlab-stats/",
        "sign": "web-ext sign --source-dir ./gitlab-stats/ --channel=listed"
    },
    "devDependencies": {
        "web-ext": "^7.6.0"
    }
}
